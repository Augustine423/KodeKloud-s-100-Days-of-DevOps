apiVersion: v1
kind: service
metadata:
  name: nginx-service
spec:
  selector:
    app: nginx-deployment
  type: NodePort
  
  ports:
   - port: 80
     targetPort: 80
     nodePort: 30011
     protocol: TCP
  
